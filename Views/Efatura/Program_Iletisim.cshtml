
@{
    ViewBag.Title = "Program_Iletisim";
    Layout = "~/Views/Shread/_Layout.cshtml";
}

<section id="contact" class="contact">
    <div class="container" id="IletisimContainer">

        <div class="text-center"><h2>İLETİŞİM</h2></div>
        <br>
        <center>
            <table class="table"  id="table2">
                <thead>
                    <tr>
                        <th scope="col">BENİM FATURA</th>
                        <th scope="col"></th>

                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th scope="row">Telefon Numarası</th>
                        <td><a href="tel:+908506110645" <span>:</span> 0850 611 06 45</a></td>

                    </tr>
                    <tr>
                        <th scope="row" ">E-Mail</th>
                        <td><a href="mailto:bilgi@benimfatura.com" <span>:</span> bilgi@benimfatura.com</a></td>

                    </tr>
                    <tr>
                        <th scope="row">Destek Sistemi</th>
                        <td><a href="mailto:bilgi@benimfatura.com"<span>:</span> destek@benimfatura.com</a></td>

                    </tr>

                </tbody>
            </table>
        </center>
        <br>
        <br>
        <br>
        <div class="section-title">
            <!--  <h2>İLETİŞİM</h2>-->
            <!-- <p>İletişim bilgilerinizi doldurun biz size ulaşalım.</p>
            -->
            <form method="post" role="form" class="php-email-form">
                <div class="text-center" style="margin-top:10px; margin-bottom:20px"><h2 style="color: #5C9F24">SİZE ULAŞALIM</h2></div>
            </form>
        </div>
        <center>
            <form method="post" role="form" class="php-email-form col-lg-9">
                <div class="row">
                    <div class="col-md-6 col-lg-6 form-group">
                        <input type="text" name="ad" class="form-control" id="ad" style="color:#5786b4;border:2px solid #ced4da;" placeholder="Adınız Soyadınız:" data-rule="minlen:4" data-msg="Lütfen Boş Bırakmayınız.">
                    </div>
                    <div class="col-md-6 col-lg-6 form-group">
                        <input type="email" class="form-control" name="email" id="email" style="color:#5786b4;border:2px solid #ced4da;" placeholder="E-Mail Adresiniz:" data-rule="email" data-msg="Lütfen Boş Bırakmayınız.">
                    </div>
                </div>

                <div class="text-center" style="margin-top:30px"><button type="button" class="btn btn-success">Gönder</button></div>
            </form>
        </center>
    </div>
</section>

@section IletisimScript{
    <script>
        $("form button.btn").on("click", function () {

            let mail = {
                "ad": $("input#ad").val(),
                "email": $("input#email").val(),
                "telefon": $("input#telefon").val(),
                "konu": $("input#konu").val(),
                "mesaj": $("textarea#mesaj").val(),
            };

            toastr.success("Başarıyla mail gönderdiniz.")
            $.ajax({
                url: "@Url.Action("index","Efatura")",
                type: "post",
                dataType: "JSON",
                data: mail,
                success: function (res) {
                    console.log(res)
                    if (res.IsSuccess) {

                    } else { }
                    for (var i = 0; i < 4; i++) {
                        $("form")[i].reset()
                    }
                },
                error: function () {

                }
            });

            return false;
        })
    </script>
}